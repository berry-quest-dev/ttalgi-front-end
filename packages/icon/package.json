{
  "name": "@packages/icon",
  "version": "0.1.0",
  "main": "src/index.ts",
  "scripts": {
    "build:scripts": "tsc -p tsconfig.scripts.json",
    "generate:icons": "pnpm build:scripts && node build/generate.js",
    "generate:index": "pnpm build:scripts && node build/generate-index.js",
    "generate": "pnpm build:scripts && node build/generate-all.js",
    "watch": "pnpm build:scripts && node build/watch.js"
  },
  "devDependencies": {
    "@svgr/core": "^8.1.0",
    "@svgr/plugin-jsx": "^8.1.0",
    "@svgr/plugin-svgo": "^8.1.0",
    "@types/node": "^22.15.16",
    "chokidar": "^4.0.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
